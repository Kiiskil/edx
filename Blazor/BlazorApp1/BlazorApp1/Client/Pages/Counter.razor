@page "/counter" 
<!--Routing endpoint-->
<!--Without the (at)page directive, a component doesn't handle routed requests, but the component can still be used by other components.-->

<h1>Counter</h1>

<p>Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    //Components can also have parameters. Component parameters are defined using public properties on the component
    //class with the [Parameter] attribute. Use attributes to specify arguments for a component in markup

    //private int currentCount = 0;

    //private void IncrementCount()
    //{
    //    currentCount += 2;
    //}

    private int currentCount = 0;

    [Parameter]
    public int IncrementAmount { get; set; } = 1;

    private void IncrementCount()
    {
        currentCount += IncrementAmount;
    }
}
